This directory is reserved for static marketplace validation fixtures.

Most Phase 18A tests generate temporary package fixtures so artifact hashes can be
derived from the same deterministic package writer used by the CLI.
