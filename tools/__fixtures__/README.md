This directory is reserved for static marketplace validation fixtures.

Most tests generate temporary package fixtures so vendored content hashes can be
derived from the same deterministic directory hasher used by the CLI.
